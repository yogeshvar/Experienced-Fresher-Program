<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.listview">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        ,</activity>
        <activity android:name=".dinesh"/>
        <activity android:name=".iswaraya"/>
        <activity android:name=".maggie"/>
        <activity android:name=".pranitha"/>
        <activity android:name=".sanjeev"/>
        <activity android:name=".vishnu"/>
    </application>

</manifest>